# Автономная система верификации юридических лиц с LLM-агентом

Система автоматизирует проверку контрагентов по трём критериям (форма собственности, ОКВЭД, налоговый резидент) без внешних API, снижая время верификации с 30 мин ручной работы до 3-5 минут автоматизированного диалога.

---

## Проблема и контекст

Ручная проверка юридических лиц перед подключением к B2B-сервисам требует оператора, который сверяет описание бизнеса и/или коды ОКВЭД с разрешёнными видами деятельности, проверяет статус налогового резидента и принимает решение о подключении или отказе. Это медленно, дорого и не масштабируется.

## Архитектура решения

Решение построено на четырёх компонентах. ReAct-агент с Function Calling управляет диалогом: задаёт уточняющие вопросы, вызывает инструменты проверки, принимает финальное решение. RAG на нормативных документах даёт агенту актуальный контекст (ОКВЭД-классификатор, требования к резидентству) без дообучения модели. Локальная LLM (Qwen 2.5 14B через Ollama) работает офлайн — данные не покидают инфраструктуру. Асинхронный WebSocket-сервер на FastAPI обеспечивает неблокирующую обработку и graceful degradation при нагрузке.

## Ключевые технические решения

LLM склонна галлюцинировать решения. Function Calling решает это: агент вызывает строго типизированные функции, а решение о подключении принимается кодом, не моделью. Для контекста из нормативных документов используется RAG на Chroma. При ограниченных ресурсах CPU применяется Qwen 2.5 14B в 4-bit квантизации, и ограничение истории чата через MAX_CHAT_HISTORY. Долгие сессии бездействия обрываются автоматическим таймаутом 15 минут с очисткой контекста.

## Инструменты агента (Function Calling)

Агент располагает тремя инструментами. search_description_in_okved(query) выполняет RAG-поиск по векторному индексу ОКВЭД-классификатора и возвращает описание вида деятельности с указанием соответствия разрешённым категориям. Агент вызывает его, когда пользователь называет ОКВЭД или описывает вид деятельности, и нужно проверить его валидность. approve_user() — финальное решение о подключении пользователя к сервису, вызывается только после прохождения всех критериев верификации. reject_user(reason) — отказ в подключении с обязательным указанием причины, вызывается, если хотя бы один критерий не пройден.

Принцип разделения ответственности: LLM управляет диалогом и извлекает интенты и сущности, но критичные решения (подключить или отказать) принимаются детерминированно. approve_user и reject_user — точки невозврата, где агент передаёт управление бизнес-логике.

## Поток данных

Пользователь открывает страницу чата в браузере и устанавливает WebSocket-соединение с FastAPI-сервером. При получении сообщения сервер передаёт его в цикл ReAct-агента. Агент формирует запрос к локальной LLM (Qwen 2.5 14B), которая генерирует ответ или запрос на вызов функции. Если требуется проверка экономической  деятельности по ОКВЭД, агент вызывает search_description_in_okved, которая обращается к векторному хранилищу Chroma для поиска по классификатору. После сбора всех фактов агент принимает финальное решение через approve_user или reject_user. Ответ отправляется пользователю через WebSocket. При неактивности 15 минут соединение закрывается с уведомлением.

## Примеры диалогов

Успешная верификация:

> Бот: Здравствуйте! Я помогу вам подключиться к сервису. Для подключения к сервису, укажите юридическое лицо, краткое описание экономической деятельности и налоговое резидентство юридического лица 
> Пользователь: ООО Ромашка, разработка программного обеспечения
> [Tool call: search_description_in_okved("разработка программного обеспечения")] → Разрешённый вид деятельности  
> Бот: Подтвердите статус налогового резидента РФ  
> Пользователь: Да  
> Бот: Все критерии верификации пройдены.  
> [Tool call: approve_user()] → Подключение разрешено

Отказ в верификации:

> Бот: Здравствуйте! Я помогу вам подключиться к сервису. Для подключения к сервису, укажите юридическое лицо, краткое описание экономической деятельности и налоговое резидентство юридического лица
> Пользователь: Привет. Я работаю у ИП
> Бот: Подключение доступно только юридическим лицам. 
> [Tool call: reject_user()] → Отказано

## Стек
Python, FastAPI, WebSocket, Ollama, Qwen 2.5, Chroma, Jinja2, asyncio.

```bash
ollama pull qwen2.5:14b
pip install -r requirements.txt
python app.py